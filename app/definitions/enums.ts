export enum FirestoreCollection {
  Messages = "messages",
  PushTokens = "pushTokens",
  SubCollection = "collection",
  Threads = "threads",
  UserProfiles = "userProfiles",
}

export enum ProfilePhotoSize {
  Full = "full",
  Medium = "medium",
  Small = "small",
}

export enum ProfileMainPhotoSize {
  xxs = 40,
  xs = 50,
  sm = 60,
  md = 65,
  lg = 80,
  xl = 100,
  "2xl" = 150,
}
